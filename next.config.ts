import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable strict mode during development to prevent double-rendering
  reactStrictMode: true,
  };

export default nextConfig;
